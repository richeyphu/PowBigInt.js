{
  "name": "powbigint",
  "version": "1.0.2",
  "description": "Calculate powers of big integers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "module": "dist/index.mjs",
  "scripts": {
    "test": "jest --config jest.config.json",
    "build": "npm run bundle && npm run declaration",
    "build:clean": "rm -rf dist",
    "bundle": "npm run bundle:cjs && npm run bundle:esm",
    "bundle:cjs": "esbuild src/index.ts --bundle --minify --target=esnext --format=cjs --outfile=dist/index.js --platform=node",
    "bundle:esm": "esbuild src/index.ts --bundle --minify --target=esnext --format=esm --outfile=dist/index.mjs --platform=node",
    "declaration": "tsc src/index.ts --declaration --emitDeclarationOnly --esModuleInterop --allowSyntheticDefaultImports --outDir dist",
    "prepublish": "npm run build",
    "postpublish": "npm run build:clean"
  },
  "homepage": "https://github.com/richeyphu/PowBigInt.js#readme",
  "repository": "github:richeyphu/PowBigInt.js",
  "keywords": [
    "power",
    "big",
    "integer",
    "biginteger",
    "bigint",
    "pow",
    "math",
    "mathematics",
    "exponential",
    "utility",
    "library",
    "util",
    "lib",
    "javascript",
    "typescript"
  ],
  "author": "richeyphu",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.2.4",
    "@types/node": "^18.11.17",
    "esbuild": "0.16.10",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.4"
  },
  "files": [
    "dist/**/*"
  ]
}
